{
  "version": 1,
  "hooks": {
    "beforeShellExecution": [
      {
        "command": "node \"$(git rev-parse --show-toplevel)/scripts/agent-hooks/review-gate.mjs\" --agent=cursor",
        "matcher": "gh\\s"
      }
    ]
  }
}
