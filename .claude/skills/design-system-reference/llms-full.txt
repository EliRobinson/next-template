# Miltinson Design System — full corpus

> Miltinson Design System — tokens, React 19 components, and AI patterns for Miltinson Technologies products. Ink-led palette with one amber accent, sharp radii, hairline borders, WCAG AA by default. Packages: @elirobinson/tokens, @elirobinson/react, @elirobinson/ai-patterns (GitHub Packages registry).

Snapshot of @elirobinson/react@2.0.1 and @elirobinson/tokens@0.5.0,
generated by @elirobinson/ai-patterns@0.9.2.

If the repo you are reading this in has a different @elirobinson/react installed, this
file describes a different version than the code you are writing against. Do not trust
it — re-run:
  pnpm --package=@elirobinson/ai-patterns dlx ds-resync artifacts --write
and prefer `pnpm ds props <Name>`, which reads the installed package and cannot go stale.

## System constraints (machine-checkable ids from @elirobinson/ai-patterns/contracts)

- **no-barrel-imports** — Import via package subpaths only — @elirobinson/react/components/<tier>/<Name>, @elirobinson/react/hooks/<name>, @elirobinson/tokens/tokens.css. There are no barrel files; a bare @elirobinson/react import does not resolve. Check: Every import from an @elirobinson package names a subpath (components/*, hooks/*, styles/*.css, styles.css, tokens.css, tailwind.css, tokens.json, tokens-data, manifest, patterns, contracts, agents/*, testing/playwright).
- **no-foreign-component-libraries** — @elirobinson/react is the component vocabulary. A second library (MUI, Chakra, Ant Design, Mantine, HeroUI, Headless UI, DaisyUI) forks the system's behaviour and accessibility guarantees. Radix is already wrapped by the system, so importing it directly does the same. Check: No import resolves to a foreign component library, and no direct Radix import appears outside a repo's sanctioned gap-filler directory.
- **no-hardcoded-design-values** — Colour, radius, shadow and motion come from tokens — a mapped Tailwind utility, a .t-* class, or var(--token) in an arbitrary value. A literal cannot be re-pointed, so it silently stops responding to [data-theme="dark"] and to any token change. Check: No hex / rgb() / hsl() / oklch() literal, and no magic px or ms for radius, shadow or duration, appears in a className string, a style object, or a stylesheet that consumes values.
- **forward-ref** — Every component that renders a focusable or interactive native element uses forwardRef, forwarding to the outermost interactive element the component owns. Check: The component is wrapped in React.forwardRef and the forwarded ref resolves to the interactive DOM node (or a documented handle, e.g. VirtualListHandle).
- **touch-target-primary** — Primary interactive controls — buttons, pagination items, segmented-control options, nav items — have a minimum 44x44px touch target. Where visual density matters, keep the painted glyph small and expand the hit area with padding or a bounded overlay rather than inflating the visible control. Check: The computed hit area of each primary control is at least 44x44px.
- **touch-target-dense** — Dense inline affordances — a chip's remove glyph, a search field's clear button, rating stars, calendar day cells — follow shadcn/MUI-scale sizing rather than 44px. Reference: MUI Chip is 32px tall (24px small) with a 22px (16px small) delete icon; shadcn Badge is ~20px tall with 12px icons. Check: Dense affordances match the shadcn/MUI reference scale instead of the 44px rule.
- **hit-area-no-overlap** — An expanded hit area must never overlap sibling content. Bound the hit area — for example stretch it to the container's height, not a symmetric negative inset — so a control's overlay cannot cover an adjacent label. Check: No interactive hit area intersects a sibling element's painted or interactive area.
- **tier-boundary** — Components live under packages/react/src/components/<tier>/. If a component renders into a portal, traps focus, or manages open/closed state across multiple sub-elements, it is an organism. If it is assembled from 2+ atoms with no such orchestration, it is a molecule. Otherwise it is an atom. Check: The component's directory (atoms | molecules | organisms) matches the boundary rule for its behavior.

UI contracts: minimum touch target 44x44 (scoped — see touch-target-dense), focus-visible required: true, contrast level WCAG AA.

## Import rules

There are no barrel files. Every import names a subpath:
  import '@elirobinson/tokens/tokens.css';
  import '@elirobinson/react/styles.css';
  import { Button } from '@elirobinson/react/components/atoms/Button';
  import { useRovingFocus } from '@elirobinson/react/hooks/useRovingFocus';
A bare import from '@elirobinson/react' does not resolve.

## Design tokens (from @elirobinson/tokens/tokens.css)

- `--ink-0: #ffffff` — pure white
- `--ink-50: oklch(98.4% 0.002 247)` — off-white surface
- `--ink-100: oklch(96.2% 0.003 247)` — hairline backgrounds
- `--ink-200: oklch(92.8% 0.004 247)` — dividers
- `--ink-300: oklch(86% 0.005 247)` — borders
- `--ink-400: oklch(70% 0.006 247)` — disabled / placeholder
- `--ink-500: oklch(55% 0.008 247)` — tertiary text
- `--ink-600: oklch(42% 0.01 247)` — secondary text
- `--ink-700: oklch(30% 0.01 247)` — high-contrast secondary
- `--ink-800: oklch(20% 0.01 247)` — near-black surfaces
- `--ink-900: oklch(12% 0.008 247)` — deep surface
- `--ink-950: oklch(6% 0.006 247)` — card on dark
- `--ink-1000: #000000` — pure black
- `--signal-50: oklch(97.5% 0.025 75)`
- `--signal-100: oklch(94% 0.055 75)`
- `--signal-200: oklch(89% 0.1 75)`
- `--signal-300: oklch(83% 0.14 72)`
- `--signal-400: oklch(78% 0.16 68)`
- `--signal-500: oklch(72.5% 0.175 65)` — primary — Miltinson Amber
- `--signal-600: oklch(63% 0.165 55)`
- `--signal-700: oklch(52% 0.145 45)`
- `--signal-800: oklch(40% 0.115 40)`
- `--signal-900: oklch(28% 0.08 38)`
- `--anchor-50: oklch(96% 0.018 165)`
- `--anchor-100: oklch(91% 0.035 165)`
- `--anchor-200: oklch(82% 0.055 162)`
- `--anchor-300: oklch(70% 0.075 160)`
- `--anchor-400: oklch(55% 0.085 158)`
- `--anchor-500: oklch(42% 0.08 156)` — primary — Miltinson Forest
- `--anchor-600: oklch(34% 0.065 156)`
- `--anchor-700: oklch(26% 0.05 156)`
- `--anchor-800: oklch(19% 0.035 156)`
- `--anchor-900: oklch(13% 0.025 156)`
- `--status-success: oklch(62% 0.16 155)`
- `--status-warning: oklch(78% 0.16 85)`
- `--status-danger: oklch(58% 0.22 25)`
- `--status-info: oklch(60% 0.13 240)`
- `--bg: var(--ink-0)` — page background
- `--bg-subtle: var(--ink-50)` — zebra / panel
- `--bg-muted: var(--ink-100)` — code blocks, inset wells
- `--bg-inverse: var(--ink-1000)` — dark hero, footer
- `--surface: var(--ink-0)` — cards
- `--surface-2: var(--ink-50)` — nested cards
- `--surface-3: var(--ink-100)` — deeper inset
- `--border: var(--ink-200)` — default hairline
- `--border-strong: var(--ink-300)` — emphasized
- `--border-inverse: oklch(100% 0 0 / 0.12)` — on dark
- `--fg: var(--ink-1000)` — primary text
- `--fg-1: var(--ink-1000)` — alias
- `--fg-2: var(--ink-600)` — secondary text
- `--fg-3: var(--ink-500)` — tertiary / meta
- `--fg-4: var(--ink-400)` — disabled
- `--fg-inverse: var(--ink-0)` — on dark
- `--fg-on-signal: var(--ink-1000)` — text on lime
- `--accent: var(--signal-500)` — Miltinson Amber
- `--accent-hover: var(--signal-400)`
- `--accent-press: var(--signal-600)`
- `--accent-fg: var(--ink-1000)`
- `--accent-tint: var(--signal-50)` — very-light amber backgrounds
- `--anchor: var(--anchor-500)` — Miltinson Forest
- `--anchor-hover: var(--anchor-400)`
- `--anchor-press: var(--anchor-600)`
- `--anchor-fg: var(--ink-0)`
- `--anchor-tint: var(--anchor-50)`
- `--focus-ring: var(--ink-1000)` — high-contrast focus, ink-on-bg
- `--link: var(--ink-1000)`
- `--link-hover: var(--signal-700)`
- `--link-visited: var(--ink-700)`
- `--status-success: var(--anchor-500)`
- `--status-warning: var(--signal-600)`
- `--font-sans: var(--ds-font-sans-override, 'Geist', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif)`
- `--font-display: var(--ds-font-display-override, 'Geist', ui-sans-serif, system-ui, -apple-system, sans-serif)`
- `--font-mono: var(--ds-font-mono-override, 'JetBrains Mono', ui-monospace, 'SF Mono', Menlo, Consolas, monospace)`
- `--fs-3xs: 10px`
- `--fs-2xs: 11px`
- `--fs-xs: 12px`
- `--fs-sm: 14px`
- `--fs-md: 16px` — base
- `--fs-lg: 18px`
- `--fs-xl: 20px`
- `--fs-2xl: 24px`
- `--fs-3xl: 30px`
- `--fs-4xl: 38px`
- `--fs-5xl: 48px`
- `--fs-6xl: 64px`
- `--fs-7xl: 84px`
- `--fs-8xl: 112px`
- `--lh-tight: 1.05`
- `--lh-snug: 1.2`
- `--lh-normal: 1.5`
- `--lh-relaxed: 1.65`
- `--tr-tight: -0.025em`
- `--tr-snug: -0.015em`
- `--tr-normal: 0em`
- `--tr-wide: 0.04em`
- `--tr-caps: 0.08em`
- `--fw-light: 300`
- `--fw-regular: 400`
- `--fw-medium: 500`
- `--fw-semibold: 600`
- `--fw-bold: 700`
- `--fw-black: 800`
- `--space-0: 0`
- `--space-px: 1px`
- `--space-1: 4px`
- `--space-2: 8px`
- `--space-3: 12px`
- `--space-4: 16px`
- `--space-5: 20px`
- `--space-6: 24px`
- `--space-7: 32px`
- `--space-8: 40px`
- `--space-9: 48px`
- `--space-10: 64px`
- `--space-11: 80px`
- `--space-12: 96px`
- `--space-13: 128px`
- `--space-14: 160px`
- `--space-15: 192px`
- `--radius-none: 0`
- `--radius-xs: 2px`
- `--radius-sm: 4px` — default for inputs, buttons
- `--radius-md: 6px` — cards
- `--radius-lg: 10px` — modals
- `--radius-xl: 16px` — heroes
- `--radius-pill: 999px` — tags only
- `--shadow-xs: 0 1px 0 0 oklch(0% 0 0 / 0.04)`
- `--shadow-sm: 0 1px 2px 0 oklch(0% 0 0 / 0.06), 0 0 0 1px oklch(0% 0 0 / 0.04)`
- `--shadow-md: 0 4px 12px -2px oklch(0% 0 0 / 0.08), 0 0 0 1px oklch(0% 0 0 / 0.04)`
- `--shadow-lg: 0 12px 32px -8px oklch(0% 0 0 / 0.14), 0 0 0 1px oklch(0% 0 0 / 0.05)`
- `--shadow-xl: 0 24px 56px -12px oklch(0% 0 0 / 0.2), 0 0 0 1px oklch(0% 0 0 / 0.05)`
- `--shadow-inset: inset 0 1px 0 0 oklch(0% 0 0 / 0.04)`
- `--shadow-focus: 0 0 0 2px var(--bg), 0 0 0 4px var(--focus-ring)`
- `--ease-out: cubic-bezier(0.22, 1, 0.36, 1)`
- `--ease-in-out: cubic-bezier(0.65, 0, 0.35, 1)`
- `--ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1)`
- `--dur-instant: 80ms`
- `--dur-fast: 140ms`
- `--dur-normal: 220ms`
- `--dur-slow: 420ms`
- `--container-sm: 640px`
- `--container-md: 768px`
- `--container-lg: 1024px`
- `--container-xl: 1280px`
- `--container-2xl: 1440px`
- `--gutter: max(20px, 4vw)`
- `--z-base: 0`
- `--z-raised: 10`
- `--z-sticky: 100`
- `--z-overlay: 1000`
- `--z-modal: 1100`
- `--z-toast: 1200`
- `--z-tooltip: 1300`

## Components

### Avatar (atoms)

Image avatar with initials fallback, in sm/md/lg sizes.

Import: `import { Avatar, AvatarImage, AvatarFallback } from '@elirobinson/react/components/atoms/Avatar';`
Styles: @elirobinson/react/styles/atoms/Avatar.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| alt | `string` | no |  | — |
| fallback | `string` | no | — | — |
| size | `"sm" \| "md" \| "lg"` | no | md | — |
| src | `string` | no | — | — |

#### AvatarImage

(no props of its own beyond inherited HTML attributes)
Also accepts all `ImgHTMLAttributes<HTMLImageElement>` props.

#### AvatarFallback

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLSpanElement>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Badge (atoms)

Small status label — default, signal, anchor, solid, and outline variants.

Import: `import { Badge } from '@elirobinson/react/components/atoms/Badge';`
Styles: @elirobinson/react/styles/atoms/Badge.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLSpanElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| variant | `"default" \| "signal" \| "anchor" \| "solid" \| "outline"` | no | default | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Button (atoms)

The primary action control — primary, accent, secondary, and ghost variants in three sizes.

Import: `import { Button } from '@elirobinson/react/components/atoms/Button';`
Styles: @elirobinson/react/styles/atoms/Button.css (included in styles.css)
Also accepts all `ButtonHTMLAttributes<HTMLButtonElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| size | `"sm" \| "md" \| "lg"` | no | md | — |
| variant | `"primary" \| "accent" \| "secondary" \| "ghost"` | no | primary | — |

Constraints: no-barrel-imports, forward-ref, touch-target-primary, hit-area-no-overlap (see System constraints)

### Checkbox (atoms)

Labelled checkbox with a full-height 44px click row.

Import: `import { Checkbox } from '@elirobinson/react/components/atoms/Checkbox';`
Styles: @elirobinson/react/styles/atoms/Checkbox.css (included in styles.css)
Also accepts all `Omit<InputHTMLAttributes<HTMLInputElement>, 'type'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| label | `string` | yes | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Eyebrow (atoms)

Uppercase mono section label — marketing typography.

Import: `import { Eyebrow } from '@elirobinson/react/components/atoms/Eyebrow';`
Styles: @elirobinson/react/styles/atoms/Eyebrow.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLSpanElement>` props.

(no props of its own beyond inherited HTML attributes)

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Input (atoms)

Batteries-included labelled text input with hint and error wiring.

Import: `import { Input } from '@elirobinson/react/components/atoms/Input';`
Styles: @elirobinson/react/styles/atoms/field.css (included in styles.css)
Also accepts all `InputHTMLAttributes<HTMLInputElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| label | `string` | yes | — | — |
| error | `string` | no | — | — |
| hint | `string` | no | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Kbd (atoms)

Styled kbd element for keyboard-shortcut hints.

Import: `import { Kbd } from '@elirobinson/react/components/atoms/Kbd';`
Styles: @elirobinson/react/styles/atoms/Kbd.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLElement>` props.

(no props of its own beyond inherited HTML attributes)

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Label (atoms)

Standalone form label for controls that manage their own wiring.

Import: `import { Label } from '@elirobinson/react/components/atoms/Label';`
Styles: @elirobinson/react/styles/atoms/field.css (included in styles.css)
Also accepts all `LabelHTMLAttributes<HTMLLabelElement>` props.

(no props of its own beyond inherited HTML attributes)

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Progress (atoms)

Determinate progress bar with an accessible value readout.

Import: `import { Progress } from '@elirobinson/react/components/atoms/Progress';`
Styles: @elirobinson/react/styles/atoms/Progress.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| value | `number` | yes | — | — |
| label | `string` | no | — | — |
| max | `number` | no | 100 | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### RadioGroup (atoms)

Context-based radio group; the group owns name, checked state, and change handling.

Import: `import { RadioGroup, RadioGroupItem } from '@elirobinson/react/components/atoms/RadioGroup';`
Styles: @elirobinson/react/styles/molecules/RuleLink.css (included in styles.css)
Also accepts all `Omit<HTMLAttributes<HTMLDivElement>, 'role'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| name | `string` | yes | — | — |
| defaultValue | `string \| number \| readonly string[]` | no | — | — |
| onValueChange | `((value: string) => void)` | no | — | — |
| value | `string` | no | — | — |

#### RadioGroupItem

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| label | `string` | yes | — | — |
| defaultValue | `string \| number \| readonly string[]` | no | — | — |
| value | `string \| number \| readonly string[]` | no | — | — |
Also accepts all `Omit<InputHTMLAttributes<HTMLInputElement>, 'type' | 'name' | 'checked' | 'onChange'>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)
Notes: ds-radio-group styles are defined in molecules/RuleLink.css, not a RadioGroup sheet

### Separator (atoms)

Horizontal or vertical hairline divider.

Import: `import { Separator } from '@elirobinson/react/components/atoms/Separator';`
Styles: @elirobinson/react/styles/atoms/Separator.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| orientation | `"horizontal" \| "vertical"` | no | horizontal | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Skeleton (atoms)

Loading placeholder block that respects reduced motion.

Import: `import { Skeleton } from '@elirobinson/react/components/atoms/Skeleton';`
Styles: @elirobinson/react/styles/atoms/Skeleton.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

(no props of its own beyond inherited HTML attributes)

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Slider (atoms)

Labelled native range input.

Import: `import { Slider } from '@elirobinson/react/components/atoms/Slider';`
Styles: @elirobinson/react/styles/atoms/Slider.css (included in styles.css)
Also accepts all `Omit<InputHTMLAttributes<HTMLInputElement>, 'type'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| label | `string` | yes | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Spinner (atoms)

Status-role loading indicator in three sizes.

Import: `import { Spinner } from '@elirobinson/react/components/atoms/Spinner';`
Styles: @elirobinson/react/styles/atoms/Spinner.css (included in styles.css)
Also accepts all `Omit<HTMLAttributes<HTMLSpanElement>, 'role'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| label | `string` | no | Loading | — |
| size | `"sm" \| "md" \| "lg"` | no | md | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Switch (atoms)

Switch-role toggle with a labelled click target.

Import: `import { Switch } from '@elirobinson/react/components/atoms/Switch';`
Styles: @elirobinson/react/styles/atoms/Switch.css (included in styles.css)
Also accepts all `Omit<InputHTMLAttributes<HTMLInputElement>, 'type' | 'role'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| label | `string` | yes | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Textarea (atoms)

Labelled multiline text field with hint and error wiring.

Import: `import { Textarea } from '@elirobinson/react/components/atoms/Textarea';`
Styles: @elirobinson/react/styles/atoms/field.css (included in styles.css)
Also accepts all `TextareaHTMLAttributes<HTMLTextAreaElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| label | `string` | yes | — | — |
| error | `string` | no | — | — |
| hint | `string` | no | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Alert (molecules)

Status callout for success, warning, danger, and info messages.

Import: `import { Alert } from '@elirobinson/react/components/molecules/Alert';`
Styles: @elirobinson/react/styles/molecules/Alert.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| title | `string` | no | — | — |
| variant | `"default" \| "success" \| "warning" \| "danger" \| "info"` | no | default | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Breadcrumb (molecules)

Page-hierarchy trail with a current-page marker.

Import: `import { Breadcrumb, BreadcrumbLink, BreadcrumbSeparator, BreadcrumbList, BreadcrumbItem } from '@elirobinson/react/components/molecules/Breadcrumb';`
Styles: @elirobinson/react/styles/molecules/Breadcrumb.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLElement>` props.

(no props of its own beyond inherited HTML attributes)

#### BreadcrumbLink

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| current | `boolean` | no | — | — |
| href | `string` | no | # | — |
Also accepts all `HTMLAttributes<HTMLSpanElement>` props.

#### BreadcrumbSeparator

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLSpanElement>` props.

#### BreadcrumbList

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLOListElement>` props.

#### BreadcrumbItem

(no props of its own beyond inherited HTML attributes)
Also accepts all `LiHTMLAttributes<HTMLLIElement>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Card (molecules)

Bordered content surface with header, title, description, content, and footer subcomponents.

Import: `import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@elirobinson/react/components/molecules/Card';`
Styles: @elirobinson/react/styles/molecules/Card.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

(no props of its own beyond inherited HTML attributes)

#### CardHeader

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### CardTitle

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLHeadingElement>` props.

#### CardDescription

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLParagraphElement>` props.

#### CardContent

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### CardFooter

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Chip (molecules)

Compact tag with an optional dense remove button.

Import: `import { Chip } from '@elirobinson/react/components/molecules/Chip';`
Styles: @elirobinson/react/styles/molecules/Chip.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLSpanElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| onRemove | `(() => void)` | no | — | — |
| removeLabel | `string` | no | — | Accessible name for the remove button. Defaults to `Remove ${children}`,
which only produces a correct label when `children` is a plain string.
Pass this explicitly when `children` is an element (e.g. `<b>Design</b>`). |

Constraints: no-barrel-imports, forward-ref, touch-target-dense, hit-area-no-overlap (see System constraints)

### EmptyState (molecules)

Placeholder for empty data — title, description, icon, and action slot.

Import: `import { EmptyState } from '@elirobinson/react/components/molecules/EmptyState';`
Styles: @elirobinson/react/styles/molecules/EmptyState.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| action | `ReactNode` | no | — | — |
| description | `string` | no | — | — |
| icon | `ReactNode` | no | — | — |
| title | `string` | no | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### FormField (molecules)

Label, hint, and error wrapper for controls that don't wire their own accessibility.

Import: `import { FormField } from '@elirobinson/react/components/molecules/FormField';`
Styles: @elirobinson/react/styles/molecules/FormField.css (included in styles.css)
Also accepts all `Omit<HTMLAttributes<HTMLDivElement>, 'children'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| htmlFor | `string` | yes | — | — |
| label | `string` | yes | — | — |
| error | `string` | no | — | — |
| hint | `string` | no | — | — |
| required | `boolean` | no | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Pagination (molecules)

Page-button list with aria-current marking and 44px targets.

Import: `import { Pagination } from '@elirobinson/react/components/molecules/Pagination';`
Styles: @elirobinson/react/styles/molecules/Pagination.css (included in styles.css)
Also accepts all `Omit<HTMLAttributes<HTMLElement>, 'onChange'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| onPageChange | `(page: number) => void` | yes | — | — |
| page | `number` | yes | — | — |
| pageCount | `number` | yes | — | — |

Constraints: no-barrel-imports, forward-ref, touch-target-primary, hit-area-no-overlap (see System constraints)

### Rating (molecules)

Star rating — read-only by default, interactive when given a change handler.

Import: `import { Rating } from '@elirobinson/react/components/molecules/Rating';`
Styles: @elirobinson/react/styles/molecules/Rating.css (included in styles.css)
Also accepts all `Omit<HTMLAttributes<HTMLDivElement>, 'role'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| value | `number` | yes | — | — |
| max | `number` | no | 5 | — |
| onValueChange | `((value: number) => void)` | no | — | — |

Constraints: no-barrel-imports, forward-ref, touch-target-dense, hit-area-no-overlap (see System constraints)

### RuleLink (molecules)

Ink-underline arrow link for marketing pages.

Import: `import { RuleLink } from '@elirobinson/react/components/molecules/RuleLink';`
Styles: @elirobinson/react/styles/molecules/RuleLink.css (included in styles.css)
Also accepts all `AnchorHTMLAttributes<HTMLAnchorElement>` props.

(no props of its own beyond inherited HTML attributes)

Constraints: no-barrel-imports, forward-ref (see System constraints)

### SearchField (molecules)

Search input with a built-in dense clear button.

Import: `import { SearchField } from '@elirobinson/react/components/molecules/SearchField';`
Styles: @elirobinson/react/styles/molecules/SearchField.css (included in styles.css)
Also accepts all `Omit<InputHTMLAttributes<HTMLInputElement>, 'type' | 'onChange' | 'value' | 'defaultValue'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| defaultValue | `string` | no | — | — |
| onValueChange | `((value: string) => void)` | no | — | — |
| value | `string` | no | — | — |

Constraints: no-barrel-imports, forward-ref, touch-target-dense, hit-area-no-overlap (see System constraints)

### SegmentedControl (molecules)

Radio-style option group with arrow-key navigation and 44px targets.

Import: `import { SegmentedControl } from '@elirobinson/react/components/molecules/SegmentedControl';`
Styles: @elirobinson/react/styles/molecules/SegmentedControl.css (included in styles.css)
Also accepts all `Omit<HTMLAttributes<HTMLDivElement>, 'onChange' | 'role'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| onValueChange | `(value: string) => void` | yes | — | — |
| options | `SegmentedControlOption[]` | yes | — | — |
| value | `string` | yes | — | — |

Constraints: no-barrel-imports, forward-ref, touch-target-primary, hit-area-no-overlap (see System constraints)

### Stepper (molecules)

Ordered-list progress indicator for multi-step flows.

Import: `import { Stepper } from '@elirobinson/react/components/molecules/Stepper';`
Styles: @elirobinson/react/styles/molecules/Stepper.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLOListElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| activeStep | `number` | yes | — | — |
| steps | `StepperStep[]` | yes | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Accordion (organisms)

Expandable sections with configurable heading levels.

Import: `import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from '@elirobinson/react/components/organisms/Accordion';`
Styles: @elirobinson/react/styles/organisms/Accordion.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLDivElement> & AccordionSharedProps` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| defaultValue | `string \| (readonly string[] & string) \| ((string \| number \| readonly string[]) & string[])` | no | — | — |
| headingLevel | `1 \| 2 \| 3 \| 4 \| 5 \| 6` | no | 3 | Heading level (1-6) that wraps each trigger, per the WAI-ARIA accordion
pattern. There is no single correct default for an arbitrary document
outline, so this is exposed rather than hardcoded — a consumer nesting
an Accordion under an `<h2>` section should pass `headingLevel={3}` (the
default) while one nesting it directly under the page `<h1>` should pass
`headingLevel={2}`. Defaults to 3, the common case (accordion under a
page section heading). |
| onValueChange | `((value: string) => void) \| ((value: string[]) => void)` | no | — | — |
| type | `"multiple" \| "single"` | no | single | — |
| value | `string \| string[]` | no | — | — |

#### AccordionItem

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| value | `string` | yes | — | — |
| defaultValue | `string \| (readonly string[] & string) \| ((string \| number \| readonly string[]) & string[])` | no | — | — |
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### AccordionTrigger

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| defaultValue | `string \| (readonly string[] & string) \| ((string \| number \| readonly string[]) & string[])` | no | — | — |
| onClick | `MouseEventHandler<HTMLButtonElement>` | no | — | — |
Also accepts all `Omit<ButtonHTMLAttributes<HTMLButtonElement>, 'id' | 'onClick' | 'aria-expanded' | 'aria-controls'>` props.

#### AccordionContent

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| defaultValue | `string \| (readonly string[] & string) \| ((string \| number \| readonly string[]) & string[])` | no | — | — |
Also accepts all `Omit<HTMLAttributes<HTMLDivElement>, 'id' | 'role' | 'aria-labelledby'>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Combobox (organisms)

Filterable single-select combobox with a virtualized listbox popup.

Import: `import { Combobox } from '@elirobinson/react/components/organisms/Combobox';`
Styles: @elirobinson/react/styles/organisms/Combobox.css, @elirobinson/react/styles/organisms/VirtualList.css (included in styles.css)

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| label | `string` | yes | — | Accessible label for the combobox input (also its accessible name). |
| onValueChange | `(value: string) => void` | yes | — | — |
| options | `ComboboxOption[]` | yes | — | — |
| className | `string` | no | — | — |
| value | `string` | no | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### CommandPalette (organisms)

Dialog-based command list with search filtering and keyboard-shortcut hints.

Import: `import { CommandPalette } from '@elirobinson/react/components/organisms/CommandPalette';`
Styles: @elirobinson/react/styles/organisms/CommandPalette.css, @elirobinson/react/styles/organisms/Dialog.css (included in styles.css)

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| commands | `CommandPaletteCommand[]` | yes | — | — |
| onOpenChange | `(open: boolean) => void` | yes | — | — |
| open | `boolean` | yes | — | — |
| className | `string` | no | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### DatePicker (organisms)

Popover date grid using the ARIA grid pattern.

Import: `import { DatePicker } from '@elirobinson/react/components/organisms/DatePicker';`
Styles: @elirobinson/react/styles/organisms/DatePicker.css (included in styles.css)

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| label | `string` | yes | — | Accessible label for the trigger input (also its accessible name). |
| onValueChange | `(date: Date) => void` | yes | — | — |
| className | `string` | no | — | — |
| value | `Date` | no | — | — |

Constraints: no-barrel-imports, forward-ref, touch-target-dense, hit-area-no-overlap (see System constraints)

### Dialog (organisms)

Modal dialog built on the native dialog element.

Import: `import { Dialog, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogTrigger, DialogContent, DialogClose } from '@elirobinson/react/components/organisms/Dialog';`
Styles: @elirobinson/react/styles/organisms/Dialog.css (included in styles.css)

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| defaultOpen | `boolean` | no | false | — |
| onOpenChange | `((open: boolean) => void)` | no | — | — |
| open | `boolean` | no | — | — |

#### DialogHeader

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### DialogTitle

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLHeadingElement>` props.

#### DialogDescription

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLParagraphElement>` props.

#### DialogFooter

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### DialogTrigger

(no props of its own beyond inherited HTML attributes)
Also accepts all `ButtonHTMLAttributes<HTMLButtonElement>` props.

#### DialogContent

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDialogElement>` props.

#### DialogClose

(no props of its own beyond inherited HTML attributes)
Also accepts all `ButtonHTMLAttributes<HTMLButtonElement>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)

### DropdownMenu (organisms)

Keyboard-navigable action menu anchored to a trigger.

Import: `import { DropdownMenu, DropdownMenuContent, DropdownMenuLabel, DropdownMenuSeparator, DropdownMenuTrigger, DropdownMenuItem } from '@elirobinson/react/components/organisms/DropdownMenu';`
Styles: @elirobinson/react/styles/organisms/DropdownMenu.css (included in styles.css)

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| defaultOpen | `boolean` | no | false | — |
| onOpenChange | `((open: boolean) => void)` | no | — | — |
| open | `boolean` | no | — | — |

#### DropdownMenuContent

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### DropdownMenuLabel

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| inset | `boolean` | no | — | — |
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### DropdownMenuSeparator

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### DropdownMenuTrigger

(no props of its own beyond inherited HTML attributes)
Also accepts all `ButtonHTMLAttributes<HTMLButtonElement>` props.

#### DropdownMenuItem

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| inset | `boolean` | no | — | — |
Also accepts all `ButtonHTMLAttributes<HTMLButtonElement>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)

### NavigationMenu (organisms)

Always-rendered nested link list with current-page marking.

Import: `import { NavigationMenu } from '@elirobinson/react/components/organisms/NavigationMenu';`
Styles: @elirobinson/react/styles/organisms/NavigationMenu.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| items | `NavigationMenuItem[]` | yes | — | — |
| currentPath | `string` | no | — | — |

Constraints: no-barrel-imports, forward-ref, touch-target-primary, hit-area-no-overlap (see System constraints)

### Popover (organisms)

Anchored floating panel with outside-click and Escape dismissal.

Import: `import { Popover, PopoverContent, PopoverTrigger } from '@elirobinson/react/components/organisms/Popover';`
Styles: @elirobinson/react/styles/organisms/Popover.css (included in styles.css)

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| defaultOpen | `boolean` | no | false | — |
| onOpenChange | `((open: boolean) => void)` | no | — | — |
| open | `boolean` | no | — | — |

#### PopoverContent

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### PopoverTrigger

(no props of its own beyond inherited HTML attributes)
Also accepts all `ButtonHTMLAttributes<HTMLButtonElement>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Select (organisms)

Native select with label, hint, and error wiring.

Import: `import { Select } from '@elirobinson/react/components/organisms/Select';`
Styles: @elirobinson/react/styles/atoms/field.css (included in styles.css)
Also accepts all `SelectHTMLAttributes<HTMLSelectElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| label | `string` | yes | — | — |
| error | `string` | no | — | — |
| hint | `string` | no | — | — |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Sheet (organisms)

Edge drawer built on the native dialog element.

Import: `import { Sheet, SheetHeader, SheetTitle, SheetDescription, SheetFooter, SheetTrigger, SheetContent, SheetClose } from '@elirobinson/react/components/organisms/Sheet';`
Styles: @elirobinson/react/styles/organisms/Sheet.css (included in styles.css)

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| defaultOpen | `boolean` | no | false | — |
| onOpenChange | `((open: boolean) => void)` | no | — | — |
| open | `boolean` | no | — | — |

#### SheetHeader

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### SheetTitle

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLHeadingElement>` props.

#### SheetDescription

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLParagraphElement>` props.

#### SheetFooter

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### SheetTrigger

(no props of its own beyond inherited HTML attributes)
Also accepts all `ButtonHTMLAttributes<HTMLButtonElement>` props.

#### SheetContent

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| side | `"left" \| "right" \| "top" \| "bottom"` | no | right | — |
Also accepts all `HTMLAttributes<HTMLDialogElement>` props.

#### SheetClose

(no props of its own beyond inherited HTML attributes)
Also accepts all `ButtonHTMLAttributes<HTMLButtonElement>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Table (organisms)

Paginated data table backed by `@tanstack/react-table`'s row models.

For large row counts, use `VirtualTable` instead — windowing and pagination
are two strategies for the same problem, so neither component combines them.

Import: `import { Table } from '@elirobinson/react/components/organisms/Table';`
Styles: @elirobinson/react/styles/organisms/Table.css, @elirobinson/react/styles/organisms/table/core.css (included in styles.css)
Also accepts all `TableElementProps & TableBaseProps<T>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| columns | `ColumnDef<T, unknown>[]` | yes | — | — |
| data | `T[]` | yes | — | — |
| emptyMessage | `string` | no | — | Title shown by the EmptyState row when there are no rows to display. |
| filterable | `boolean` | no | — | Shows a built-in global filter input above the table, wired to TanStack's filtered row model. |
| filterPlaceholder | `string` | no | — | Accessible label / placeholder for the built-in filter input. |
| pageSize | `number` | no | — | Rows per page. |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Tabs (organisms)

Tab set with roving focus and an ink underline active state.

Import: `import { Tabs, TabsList, TabsTrigger, TabsContent } from '@elirobinson/react/components/organisms/Tabs';`
Styles: @elirobinson/react/styles/organisms/Tabs.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| defaultValue | `string \| number \| readonly string[]` | no | — | — |
| onValueChange | `((value: string) => void)` | no | — | — |
| value | `string` | no | — | — |

#### TabsList

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| defaultValue | `string \| number \| readonly string[]` | no | — | — |
Also accepts all `Omit<HTMLAttributes<HTMLDivElement>, 'role'>` props.

#### TabsTrigger

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| value | `string` | yes | — | — |
| defaultValue | `string \| number \| readonly string[]` | no | — | — |
| onClick | `MouseEventHandler<HTMLButtonElement>` | no | — | — |
Also accepts all `Omit<ButtonHTMLAttributes<HTMLButtonElement>, 'value' | 'id' | 'role' | 'onClick' | 'aria-selected' | 'aria-controls' | 'tabIndex'>` props.

#### TabsContent

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| value | `string` | yes | — | — |
| defaultValue | `string \| number \| readonly string[]` | no | — | — |
Also accepts all `Omit<HTMLAttributes<HTMLDivElement>, 'id' | 'role' | 'aria-labelledby'>` props.

Constraints: no-barrel-imports (see System constraints)

### Toast (organisms)

Aria-live notification system — Toaster region plus a useToast dispatcher.

Import: `import { Toast, Toaster, ToastTitle, ToastDescription, ToastAction, useToast } from '@elirobinson/react/components/organisms/Toast';`
Styles: @elirobinson/react/styles/organisms/Toast.css (included in styles.css)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| onDismiss | `(() => void)` | no | — | — |
| variant | `"default" \| "success" \| "warning" \| "danger" \| "info"` | no | default | — |

#### Toaster

(no props of its own beyond inherited HTML attributes)

#### ToastTitle

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### ToastDescription

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### ToastAction

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLButtonElement>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)

### Tooltip (organisms)

Hover- and focus-triggered tooltip anchored to its trigger.

Import: `import { Tooltip, TooltipContent, TooltipTrigger } from '@elirobinson/react/components/organisms/Tooltip';`
Styles: @elirobinson/react/styles/organisms/Tooltip.css (included in styles.css)

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| delayDuration | `number` | no | — | — |

#### TooltipContent

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLDivElement>` props.

#### TooltipTrigger

(no props of its own beyond inherited HTML attributes)
Also accepts all `HTMLAttributes<HTMLSpanElement>` props.

Constraints: no-barrel-imports, forward-ref (see System constraints)

### VirtualList (organisms)

Windowed list that only renders the rows currently within (or near) its
viewport, backed by `@tanstack/react-virtual`. Intended to be composed by
`Table` (opt-in `virtualize` prop) and `Combobox` (long option lists) —
see the props above for what those consumers need: `height` (container
height), `estimateSize` (row height estimate), `overscan`, and
`renderItem` (render an arbitrary item by index).

The forwarded ref resolves to a {@link VirtualListHandle}, not the scroll
container element.

Import: `import { VirtualList } from '@elirobinson/react/components/organisms/VirtualList';`
Styles: @elirobinson/react/styles/organisms/VirtualList.css (included in styles.css)
Also accepts all `Omit<HTMLAttributes<HTMLDivElement>, 'children'>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| estimateSize | `(index: number) => number` | yes | — | Estimated size (px) of the row at `index`, used before it has been
measured. Required by the underlying virtualizer even for fixed-height
rows — pass a constant function (e.g. `() => 40`) in that case. |
| height | `number` | yes | — | Fixed height (px) of the scrollable viewport. |
| items | `T[]` | yes | — | The full, un-windowed list of items to virtualize. |
| renderItem | `(item: T, index: number) => ReactNode` | yes | — | Renders the item at `index`. Only called for rows in the current window. |
| overscan | `number` | no | — | Extra rows to render outside the visible viewport, above and below. |

Constraints: no-barrel-imports, forward-ref (see System constraints)

### VirtualTable (organisms)

Data table that windows its rows instead of paginating them, for large row
counts. Same column model, sorting, and filtering as `Table`; use that one
when pagination is the better fit.

Import: `import { VirtualTable } from '@elirobinson/react/components/organisms/VirtualTable';`
Styles: @elirobinson/react/styles/organisms/VirtualTable.css, @elirobinson/react/styles/organisms/VirtualList.css, @elirobinson/react/styles/organisms/table/core.css (included in styles.css)
Also accepts all `TableElementProps & TableBaseProps<T>` props.

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| columns | `ColumnDef<T, unknown>[]` | yes | — | — |
| data | `T[]` | yes | — | — |
| emptyMessage | `string` | no | — | Title shown by the EmptyState row when there are no rows to display. |
| filterable | `boolean` | no | — | Shows a built-in global filter input above the table, wired to TanStack's filtered row model. |
| filterPlaceholder | `string` | no | — | Accessible label / placeholder for the built-in filter input. |
| height | `number` | no | — | Fixed viewport height (px) for the windowed body. |
| overscan | `number` | no | — | Rows to render outside the visible viewport, above and below. |
| rowHeight | `number` | no | — | Estimated/actual row height (px) for the virtualizer. |

Constraints: no-barrel-imports, forward-ref (see System constraints)

## Hooks

### useActiveDescendant

Import: `import { useActiveDescendant } from '@elirobinson/react/hooks/useActiveDescendant';`

The `aria-activedescendant` half of the WAI-ARIA combobox/listbox pattern:
DOM focus stays on the input at all times and the "highlighted" option is
conveyed purely by id reference.

Owns one invariant that is easy to get wrong in two different places, which
is why it lives here rather than in each widget: the highlighted index must
always be inside the current option range. Filtering shrinks that range out
from under a previously-valid index, and an unclamped index hands
`aria-activedescendant` a dangling id — visually fine, silently broken for
assistive tech.

Note that the clamp is applied twice, deliberately. The *derived*
`activeIndex` is clamped so the rendered output is always valid, and
`moveActive` clamps the stored value again *before* applying its delta. Only
clamping the derived value leaves the raw state parked above the valid range,
where a single arrow key moves it from (say) 5 to 4 — both of which still
clamp to 1, so the highlight appears stuck until the user presses the key
enough times to walk back into range.

### useAnchoredPosition

Import: `import { useAnchoredPosition } from '@elirobinson/react/hooks/useAnchoredPosition';`

Positions a floating panel against its trigger with `position: fixed`, and
keeps it there while the page scrolls or the window resizes.

Geometry is written straight to the node's inline style rather than held in
state: this runs in a layout effect, before paint, so the panel never shows
up in the wrong place first. The fourth argument accepts a bare side for the
common case (`'top'` / `'bottom'`) or an options object.

It does not flip or clamp the panel to stay inside the viewport — a panel
anchored near an edge can overflow it.

### useClickOutside

Import: `import { useClickOutside } from '@elirobinson/react/hooks/useClickOutside';`

Calls back when a pointer-down lands outside the given refs — the dismissal half of every overlay.

### useDisclosure

Import: `import { useDisclosure } from '@elirobinson/react/hooks/useDisclosure';`

Open/closed state for a component that has to work both ways round: a
consumer either passes `open` and owns the value, or passes neither and lets
the component own it, seeded by `defaultOpen`.

`open` resolves to the controlled prop whenever there is one and to internal
state otherwise, and `setOpen` writes internal state only in the
uncontrolled case while always reporting through `onOpenChange` — so a
controlled consumer never ends up fighting a second copy of the truth.

`setOpen` keeps one identity for the component's lifetime; the current
`onOpenChange` is read from a ref at call time. That matters because the
dismissal hooks (`useClickOutside`, `useEscapeKey`) take a callback as a
dependency, and callers write that callback inline.

### useEscapeKey

Import: `import { useEscapeKey } from '@elirobinson/react/hooks/useEscapeKey';`

Calls back on Escape keydown while enabled — shared dismissal behavior for overlays.

### useHasMounted

Import: `import { useHasMounted } from '@elirobinson/react/hooks/useHasMounted';`

Reports `false` on the server and during the first client render, then `true`
once the component has mounted in a browser.

Gate anything that reads a browser global *during render* on this — in this
package that means `createPortal(…, document.body)`, which otherwise throws
`ReferenceError: document is not defined` when a Next.js or Remix consumer
server-renders the tree. Effects and event handlers do not need it: neither
runs on the server.

Returning `false` for the first client render (rather than checking
`typeof document` directly) is what keeps hydration clean — the client's
initial output has to match the server's, so both must skip the portal and
the content appears on the commit that follows.

### useRovingFocus

Import: `import { useRovingFocus } from '@elirobinson/react/hooks/useRovingFocus';`

Arrow/Home/End traversal for a composite widget that exposes a single tab
stop (a "roving tabindex"): Tab moves into the widget, arrows move within
it.

Both axes are accepted because the same widget can be laid out either way
and a user should not have to guess which; movement wraps at both ends,
per the WAI-ARIA authoring practices for tabs and radio groups.

Only the traversal is shared. What a move *means* differs by widget — tabs
move focus without selecting, radio-style groups select as they move — so
that decision stays with the caller in `onNavigate`.
